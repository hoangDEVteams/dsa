#include <iostream>
#include <vector>
#include <map>
using namespace std;

void coordinates(int n) {
	if (n % 2 == 0) {
		int maxvalue = 0;
		for (int i = 0; i < n; i++)
		{
			
		}
	}
	else
	{
		cout << "NO";
	}
}
int main() {
	int T, n;
	cin >> T;
	while (T -- ) {
		cin >> n;
		coordinates(n);
		cout << endl; 
	}
}